interface:
  display_name: "NuanSystem Auxiliary Master Generator"
  short_description: "Genera mantenimientos auxiliares seguros"
  brand_color: "#00B894"
  default_prompt: "Use $nuansystem-auxiliary-master-generator to create a schema 1.3 manifest with independent physical/API paths and an explicit migration date, present the SQL table and full-color compact mockup, wait for approval, and only then scaffold the auxiliary master."
